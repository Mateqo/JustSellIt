@model JustSellIt.Application.ViewModels.Product.ListProductForListVm
@{
    ViewData["Title"] = "Wyszukiwarka ogłoszeń";

}

<partial name="_AlertPanel" />

<form id="searchForm" asp-action="SearchProducts" asp-controller="Product" method="get">

    <partial name="_SearchPanel" />
    <partial name="_AdvancedSearchPanel" />

    @{
        if (Model.Count != 0)
        {
            <h1 class="add-title mt-5 mb-5" style="font-weight: bold; text-align: center">Znalezione ogłoszenia</h1>
        }
        else
        {
            <h1 class="add-title mt-5 mb-5" style="font-weight: bold; text-align: center">Nie znaleziono ogłoszeń</h1>
        }
    }

    <partial name="_ProductTile" />

    @{
        if (Model.Count != 0)
        {
            <partial name="_Pagination" />
        }
    }

</form>

@section Scripts{
    <script type="text/javascript">
        $('#searchForm').submit(function () {
            $(this)
                .find('input[name]')
                .filter(function () {
                    return !this.value;
                })
                .prop('name', '');
        });
    </script>
}